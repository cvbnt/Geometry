package c.v.b.n.t.geometry;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.view.View;

public class RoundedRectangle extends View {
    public RoundedRectangle(Context context) {
        super(context);
    }

    public RoundedRectangle(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public RoundedRectangle(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public RoundedRectangle(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
